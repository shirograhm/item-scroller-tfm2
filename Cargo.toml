[package]
name = "item_scroller_tfm2"
version = "0.2.2"
edition = "2021"

[lib]
# The built DLL must be named after the mod folder: item_scroller_tfm2.dll
crate-type = ["cdylib"]

[dependencies]
mod-api-stable = { path = "../mod-api-stable" }
serde_json = "1"

[workspace]
